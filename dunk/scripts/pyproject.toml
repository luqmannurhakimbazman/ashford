[project]
name = "dunk-ks-merge"
version = "0.1.0"
description = "Validation and test tooling for Dunk knowledge-state merges"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
