[project]
name = "dunk-local-store-tooling"
version = "0.1.0"
description = "Zero-runtime-dependency local store and legacy knowledge-state validation tooling for Dunk"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
